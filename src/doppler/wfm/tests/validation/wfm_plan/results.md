# Plan — validation report

Generated by `validate.py` in this folder. Every number is measured from the C implementation through its own binding; nothing is modelled. Re-run to regenerate.

## Executive summary

- **Status.** **CERTIFIED** — all 17 limits hold.
- **Findings.** 4 recorded, 2 still open: F1, F2.

**What a caller most needs to know**

- **The cache is indistinguishable from the composition on every axis it serves.** Baseline and every override -- snr, gain, phase, enable, seed -- reproduce a full `compose()` of the equivalently-modified scene bit for bit, across four scene shapes (§2.1, §2.2).
- **Read the drawn length off the array, never off `len()`.** For a ranged-gap scene `len()` is a worst-case capacity and each seed draws something shorter -- 8 distinct lengths over 8 seeds here. Both rectangular Monte-Carlo idioms raise on such a scene, and the docstring promises they will not (§2.4, F1).
- **A Doppler source is refused, and that is the contract working.** The cache cannot hold a channel's history, so it declines rather than serving a render that differs from `compose()` invisibly. Scoped to the behaviour, not the field: zero doppler with a declared lifetime still prepares (§2.3, F4).
- **Persist the spec, not the blob, unless the DSP cost says otherwise.** A 4096-sample two-source scene serializes to 65 KB, 99% of it cached samples, against a sub-kilobyte spec that always tracks the current DSP (§2.5).
- **Nothing gates that the restore fast path is still fast.** A fingerprint mismatch rebuilds transparently and returns identical samples, so a restore that silently stopped using its cache would pass every test in both suites (F2).

## 1. The object

`Plan` prepares a scene once and re-materializes parameter variations cheaply: the expensive DSP (spreading, pulse shaping, the LO) lives in the signal terms, which do not move when a level, a phase, the SNR or the noise seed does. The design is [docs/design/wfmgen.md](../../../../../../docs/design/wfmgen.md); the API is `native/inc/wfm/wfm_plan.h`, certified in C by `native/tests/test_wfm_plan.c`.

**This is a cache, so its subject is indistinguishability.** `Composer` is certified in its own folder and cited here rather than re-derived -- see §2.6. What is measured is whether every answer the cache serves is the answer the composition would have given, and whether what it will not serve, it refuses.

## 2. Characterisation

### 2.1 The baseline is the composition, bit for bit (C gate-0)

`render()` with no overrides against `Composer(scene).compose()` on the same scene. This is the claim the whole object rests on: a cache that is merely *close* to what it caches is a second implementation, and the campaign has seen what two copies of one formula do. Equality is exact -- `array_equal`, not `allclose`.

| scene | samples | bit-identical |
|---|---|---|
| two sources, one segment | 4096 | yes |
| two segments | 4096 | yes |
| repeats=3, fixed gap | 1152 | yes |
| repeats=3, ranged gap | 1792 | yes |

### 2.2 Every override axis is a full compose of the same change

Each axis is measured against an EXTERNAL truth -- a full `compose()` of the equivalently-modified scene -- rather than against another Plan render. A cache compared only to itself is self-consistent by construction and blind to the one defect that matters, which is that the cache and the composition have drifted apart.

| override | path | bit-identical to compose |
|---|---|---|
| snr = 6 dB | at(6, anchor_seed) | yes |
| snr = 6 dB | render(snr=6) | yes |
| tone level = -6 dBFS | render(gains=...) | yes |
| enable all-on | render(enable=...) | yes |
| phase = 0 rad | render(phases=...) | yes |
| same (snr, seed) twice | at() | yes |

**Superposition holds to 5.33e-07.** Rendering each source alone and adding the two back (less the floor, which both carry) reproduces the full render. The segment is the linear form its header claims, and this is an external truth -- the composer is never asked -- so it cannot be a defect the two share.

### 2.3 The refusals, and the edge that must NOT be refused

A cache that cannot serve a shape has two honest options and one dishonest one. `Plan` refuses, rather than serving a render that differs from `compose()` in a way nothing downstream can see. The Doppler rows are the interesting ones: the reasons are measured against `compose()` in `plan_build()` -- a channel rings the burst's tail out across the trailing gap, and `compose()` puts the AWGN inside the channel while the cache re-weights it outside -- and teaching the cache to carry that history is [gh-1109](https://github.com/doppler-dsp/doppler/issues/1109).

| out-of-scope scene | why | outcome |
|---|---|---|
| a ranged per-source field (`snr=[6, 12]`) | redrawing a source's SNR invalidates its cached render | ValueError |
| a ranged on-time (`num_samples=(1024, 2048)`) | the signal cache is fixed-length | ValueError |
| a source with `doppler` | a channel is a stateful resampler; the cache has no history | ValueError |
| a source with `doppler_rate` | same channel, same absence of history | ValueError |

The other half of a refusal is what it must NOT catch. A rule that rejects a field rather than the behaviour behind it costs a caller a scene that was always safe:

| in-scope scene | why it is in scope | outcome |
|---|---|---|
| `doppler_lifetime` declared, `doppler = 0` | zero doppler builds no channel at all | prepared |
| ranged `off_samples` / `delay_samples` | the GAPS may be ranged -- only the on-time may not | prepared |
| `repeats = 3` | bounded instancing: fresh noise over a fixed signal | prepared |

### 2.4 `len()` is a CAPACITY, and a ranged draw is shorter

For a ranged-gap scene the materialized length is a property of the DRAW, not of the scene: `len(plan)` is the worst case (every gap at its `hi` bound) and each seed lands somewhere below it. Across 8 seeds the capacity is 3072 samples and the draws take **8 distinct lengths**, from 1306 to 2314 -- 43% to 75% of capacity.

| seed | samples drawn | fraction of `len()` |
|---|---|---|
| 1 | 1837 | 0.598 |
| 2 | 1306 | 0.425 |
| 3 | 1387 | 0.451 |
| 7 | 2314 | 0.753 |
| 11 | 1833 | 0.597 |
| 13 | 1360 | 0.443 |
| 17 | 1731 | 0.563 |
| 19 | 2118 | 0.689 |

Raw sweep: [data/ranged_gap_draws.csv](data/ranged_gap_draws.csv).

**This breaks both Monte-Carlo idioms, and the docstring says it will not.** `Plan.render`'s Returns section promises `complex64` samples, *length* `len()`; for a ranged-gap scene that is false, and the consequence is not a wrong number but a raised exception at the point a campaign tries to collect its draws:

| idiom | fixed-gap scene | ranged-gap scene |
|---|---|---|
| `np.array(list(plan.monte_carlo(...)))` | (8, 1152) complex64 | **ValueError** (inhomogeneous shape) |
| preallocate `(n, len(plan))`, assign rows | ok | **ValueError** (could not broadcast) |

Recorded as **F1**. The C API is not at fault -- it returns the actual draw and documents `len()` as a capacity, and the C tests now pin that the tail past the returned length is zeroed. The gap is on the Python face, where the actual length arrives only as the array's shape and the documented contract contradicts it.

### 2.5 Persistence is an equivalence, and its cost is a size

`save()`/`PlanFromBlob` and `dump()`/`PlanFromFile` reconstruct a Plan that renders bit-identically -- baseline and override both. The cost is the reason the header calls the spec-rebuild the default: this 4096-sample two-source scene serializes to **66,320 bytes**, of which 65,536 (98.8%) is the cached sample buffers (`n_sources x num_samples x 8`). The spec that reproduces it is under a kilobyte. A blob is worth writing only when the DSP cost it saves is worth 65 KB per scene.

A truncated blob and an all-zero blob of the right length are both refused rather than reinterpreted. The endian byte and the fingerprint-mismatch rebuild are C-ONLY (F3).

### 2.6 A repeat re-rolls the noise and not the burst

Across 64 draws at one SNR no two are identical, and the ensemble mean converges on the clean render: RMS error 0.4996 over 8 draws against 0.1758 over 64, a ratio of 2.84 where independent noise predicts sqrt(8) = 2.83. That is what makes a detection rate measured over `monte_carlo()` a rate over trials rather than one trial counted 64 times. Raw figures: [data/mc_convergence.csv](data/mc_convergence.csv).

The composition's own properties -- what a segment sums to, what a gap carries, that a repeat's signal is fixed -- are certified in [wfm_compose](../wfm_compose/results.md) and cited rather than re-derived here.

## 3. Review -- findings, with verdicts

- **F1 · CONFIRMED** — **A ranged-gap Plan returns a different length for every seed, and `render()`'s documented Returns section says it does not.** The docstring promises `complex64` samples, *length* `len()`; across 8 seeds this scene returns 8 distinct lengths between 1306 and 2314 against a `len()` of 3072 (§2.4). The C API is right -- it returns the actual draw and documents `len()` as a worst-case capacity -- and the binding faithfully truncates to it; what is wrong is the Python contract written over the top. It costs a caller the two idioms a Monte-Carlo campaign reaches for first: `np.array(list(plan.monte_carlo(...)))` raises on an inhomogeneous shape, and pre-allocating `(n, len(plan))` raises on broadcast. Both are the advertised use case (`Plan` exists for Monte-Carlo campaigns) on an advertised feature (ranged gaps, redrawn per instance). Filed as [gh-1128](https://github.com/doppler-dsp/doppler/issues/1128).

- **F2 · GAP** — **Nothing can tell you the restore fast path still works.** `wfm_plan_restore` loads the cached buffers when the DSP fingerprint matches and otherwise rebuilds from the embedded spec, and both produce bit-identical output by design -- that is the feature. The consequence is that the ONLY observable difference between the fast path and the slow one is time, and the API exposes no indicator of which ran. If the fingerprint stopped matching -- a build-system change, a hash that no longer round-trips -- every restore would silently pay full `prepare()` cost, return the right samples, and pass every test in both suites. The entire point of save/restore would be dead with nothing red. Filed as [gh-1129](https://github.com/doppler-dsp/doppler/issues/1129); a one-bit accessor would make it gateable.

- **F3 · C-ONLY** — **Three claims the Python face cannot reach**, now pinned in `native/tests/test_wfm_plan.c` and certified there. (1) The zero-padded tail: the C `render()`/`at()` write up to `len()` and zero everything past the returned draw, which the binding hides by truncating to the actual length -- so the guarantee a C caller reusing one buffer depends on is invisible from Python. (2) `destroy(NULL)` as a documented no-op, which has no Python spelling. (3) The foreign-endian byte at blob offset 6: Python covers a truncated and a wrong-magic blob, and the endian guard -- the one that stops native-endian POD buffers being reinterpreted -- was covered by neither suite until now. All three proven by sabotage.

- **F4 · BY DESIGN** — **The Doppler refusal is the honest half of the cache's contract, not a missing feature.** A Doppler channel is a stateful resampler that runs through the gaps, so what a burst renders as depends on the leading delay and the previous instance's gap; the cache holds one clean on-time in isolation and has nowhere to keep that history. It also puts the AWGN outside the channel where `compose()` puts it inside. Both were measured against `compose()` rather than assumed (§2.3), and a cached render that differs from `compose()` invisibly is worse than no plan. Carrying the history is [gh-1109](https://github.com/doppler-dsp/doppler/issues/1109). The refusal is correctly scoped to the behaviour and not to the field: a `doppler_lifetime` on a source with zero doppler builds no channel and still prepares.

## 4. Limits -- the certified envelope

Claims a caller may rely on. A failure here is a regression, not a new finding. Every one is asserted by `src/doppler/wfm/tests/test_validation_limits.py`.


| verdict | claim |
|---|---|
| PASS | the baseline render is bit-identical to a full compose across all four scene shapes -- one segment, two segments, fixed-gap repeats and ranged-gap repeats |
| PASS | every override axis reproduces a full compose of the equivalently-modified scene, bit for bit: snr through both at() and render(), a non-anchor gain, enable, and phase |
| PASS | phase = 0 is exactly the identity, not merely close to it |
| PASS | superposition holds to 5.3e-07: the sources rendered alone add back to the full render, so the segment is the linear form its header claims |
| PASS | every out-of-scope scene is REFUSED with ValueError -- a ranged per-source field, a ranged on-time, and a source carrying doppler or doppler_rate |
| PASS | and the refusal is scoped to the behaviour, not the field: a lifetime with zero doppler, ranged gaps, and repeats all still prepare |
| PASS | len() is a worst-case capacity (3072 samples here) and no draw ever exceeds it |
| PASS | a ranged-gap scene draws a DIFFERENT length per seed (8 distinct over 8 seeds) -- the materialized length is a property of the draw, and a caller must read it from the array rather than from len() |
| PASS | a scene with no ranged gap draws one fixed length, so the rectangular Monte-Carlo idiom is safe exactly there |
| PASS | a Plan restored from save() renders the baseline bit-identically |
| PASS | and serves an override bit-identically too -- the restored cache is the cache, not an approximation of it |
| PASS | dump() -> PlanFromFile round-trips to the same bytes as the in-memory blob |
| PASS | a truncated blob and an all-zero blob of the right length are both refused, not reinterpreted |
| PASS | the blob is dominated by the cached buffers (98.8% of 66,320 bytes) -- the spec-rebuild path is the cheap default for a reason |
| PASS | no two Monte-Carlo draws are identical: each carries fresh noise over the fixed signal |
| PASS | and the ensemble mean converges on the clean render at the independent-noise rate (ratio 2.84 over an 8x ensemble increase, against sqrt(8) = 2.83), so a rate measured over draws is a rate over trials |
| PASS | the composition's own envelope is cited rather than re-derived -- counted, so a part quietly re-measured here is a change |


## 5. Summary

- **4 findings**, 2 of them gaps or confirmed defects: F1, F2
- **17/17 limits** hold
