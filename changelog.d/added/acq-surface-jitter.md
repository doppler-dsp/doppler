- **The searcher's surface as a code tracker, measured**
    (`validate_acq_surface_jitter`, design §12.19): the early/prompt/late
    discriminator read off the correlation surface at an emitter's cell,
    inverted through its measured S-curve, reads the code phase at 0.008
    chip per 31.5 ms dwell in the code-only window at 45 dB-Hz under 18 ppm,
    beside the DLL's 0.013 per epoch, with the Doppler to 0.5 Hz from the
    rows; under data the tile's rows summed as power read it at 0.09 chip
    per dwell. The `--check` is in the C suite.
