# wfm_writer.pyi — type stubs for the wfm_writer handle extension.
#
# Generated by just-makeit (gh-306). `Writer` is a typed CPython
# class over an opaque wfm_writer_t resource handle.
from __future__ import annotations

from typing import Any

import numpy as np
from numpy.typing import NDArray

class Writer:
    def __init__(self, path: str, file_type: str = ..., sample_type: str = ..., endian: str = ..., fs: float = ..., fc: float = ..., total: int = ..., headroom: float = ...) -> None: ...
    def write(self, x: NDArray[Any]) -> int: ...
    def track_clipping(self, on: int = ...) -> None: ...
    @property
    def clip_fraction(self) -> float: ...
    @property
    def peak_dbfs(self) -> float: ...
    @property
    def clipped(self) -> int: ...
    def close(self) -> None: ...
    def __enter__(self) -> Writer: ...
    def __exit__(self, *exc: Any) -> None: ...
