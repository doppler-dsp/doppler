collect_ignore = [
    # In-progress reference implementation — not yet syntactically complete
    "doppler/resample/reference.py",
    # Examples are runnable scripts, not test modules
    "doppler/examples/ctypes_demo.py",
]
