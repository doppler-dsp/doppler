/*
 * resample_core.c — Module-level design helpers for the resample module.
 *
 * Implementations live in the per-function .c files (ciccompmf.c,
 * kaiser_beta.c, kaiser_num_taps.c) per the jm 0.14 no-splicing layout.
 */
#include "resample/resample_core.h"
