# Gold — validation report

Generated by `validate.py` in this folder. Every number is measured from the C implementation through its own binding; nothing is modelled. Re-run to regenerate.

## Executive summary

- **Status.** **CERTIFIED** — all 17 limits hold.
- **Findings.** 4 recorded, 1 still open: F4.

**What a caller most needs to know**

- **There are 1023 codes, not 1024.** The header said 1024, which was neither the reachable count (2^n - 1, since a zero seed is refused) nor the family size (2^n + 1 = 1025). Corrected and pinned; allocate against 1023 (§2.1, F1).
- **Design the receiver to a 65-chip sidelobe.** Peak 1023 against worst sidelobe 65 is 23.9 dB, and every correlation value in the family falls in {-1, -65, 63} = the theoretical {-1, -t, t-2} at t = 65 (§2.2).
- **That bound is what a family costs.** A plain m-sequence of the same period has a worst sidelobe of 1, 36.3 dB better. One transmitter should use the m-sequence; a shared band cannot, because two m-sequences have no bounded cross-correlation (§2.2).
- **The default configuration is the CCSDS worked example.** `Gold()` reproduces PN Code Library #365 chip for chip, which is the conformance anchor for taps that are otherwise unverifiable from inside this repository (§2.3, F4).
- **The preferred-pair property is sampled, not exhaustive.** 66 pairs here and 15 in C, out of 522,753. No gate pays for the full set, and the report says so rather than implying otherwise (F3).

## 1. The object

`Gold` is the CCSDS command-link Gold code generator (CCSDS 415.0-G-1 section 5.2.2.4, Figure 5-1): two same-clocked Fibonacci LFSRs with fixed feedback polynomials, XOR-combined chip by chip into a 1023-chip code. Register B is fixed by the standard; Register A's initial value is "User dependent" and is what selects a family member. The design is [docs/design/wfmgen.md](../../../../../../docs/design/wfmgen.md); the API is `native/inc/gold/gold_core.h`, certified in C by `native/tests/test_gold_core.c`.

**Its subject is the family.** One Gold code is unremarkable; the construction exists because a preferred pair generates a whole SET of codes with a bounded mutual correlation, so many transmitters can share a band. What is measured here is how many codes there are and what the bound costs.

## 2. Characterisation

### 2.1 The family is 1023 codes, and the header said 1024

Every nonzero `seed_a` was generated and compared: **1023 seeds produce 1023 distinct codes**. The header's *"walks the whole Gold-code family (2^length members)"* was wrong in both directions at once, and this certification corrected it (F1):

| quantity | value | why |
|---|---|---|
| seeds available | 1023 | `gold_create` rejects a zero seed, so 2^length **- 1** |
| distinct codes reached | 1023 | measured, one per seed |
| header's old figure | 1024 | **neither** the reachable count nor the family size |
| true family size | 1025 | 2^n **+ 1**: these plus the two constituent m-sequences |

The two the generator cannot reach are Register A's and Register B's own m-sequences — it always XORs both registers, so no seed produces either alone. That is a property of this generator, not of Gold codes, and it is now said out loud in the header because a caller sizing a code-assignment scheme allocates against that number. Across a 12-member sample no code is a cyclic shift of another either, so the count is codes and not phases of fewer codes.

### 2.2 The bound, and what it costs

Every correlation value across 66 cross-correlated pairs and one member's full autocorrelation — 68,540 values in all — falls in a set of exactly **3**: `[-65, -1, 63]`.

| correlation value | count | fraction |
|---|---|---|
| -65 | 7861 | 0.115 |
| -1 | 51537 | 0.752 |
| 63 | 9142 | 0.133 |

The header states those three numbers without saying where they come from. They are `t(n) = 2^((n+2)/2) + 1` for even `n`, which at n=10 is **65**, giving the set `{-1, -t, t-2}` = `{-1, -65, 63}`. Deriving it rather than pattern-matching the literals is what makes the check meaningful at a different length.

**The cost is the near-far margin**, and it is the whole design trade. A correlator sees a peak of 1023 against a worst sidelobe of 65 — **23.9 dB**. The same length of plain m-sequence has a worst sidelobe of 1, i.e. 60.2 dB. Gold codes buy a family of 1023 mutually-bounded codes and pay 36.3 dB of correlation purity for it. If only one transmitter is ever on the air, an m-sequence is the better code; the moment a second appears, its cross-correlation is unbounded and Gold is the answer. Raw histogram: [data/correlation_histogram.csv](data/correlation_histogram.csv).

### 2.3 The standard's own vector

CCSDS 415.0-G-1 Figure 5-2's worked example — PN Code Library Table 1, Code Number 365, reached with the default `seed_a = 350` — reproduces chip for chip over its first 15, and the code is balanced at 512 ones against 511 zeros. That vector is the conformance anchor: the taps and initial values here are not derivable from anything in this repository, so agreeing with the published sequence is what says they were transcribed correctly.

The document itself is not in the repository's standards library, so this report certifies the implementation against the published vector rather than against the prose. That is a limit of the evidence and it is recorded as such (F4).

### 2.4 Reset and resume

`reset()` reloads both registers so the sequence restarts from chip 0, reproducing a full period bit-for-bit, and a serialized blob resumes a mid-code generator bit-exactly into a fresh instance. Both registers are in the blob; restoring only one would desynchronise the XOR and is caught by the round-trip.

## 3. Review -- findings, with verdicts

- **F1 · FIXED** — **The header's family size was wrong in both directions at once.** It said varying `seed_a` *"walks the whole Gold-code family (2^length members)"* -- 1024 at length=10 -- and repeated "the 1024-code Gold family" in `@param seed_a`. Measured over every nonzero seed: **1023** distinct codes. Only 2^length **- 1** seeds exist, because `gold_create` rejects zero; and the classical Gold set for a preferred pair has 2^n **+ 1** = 1025 members -- these plus the two constituent m-sequences, which this generator can never emit because it always XORs both registers. So 1024 was neither the reachable count nor the family size. Corrected in the header (and carried into `wfm.pyi` by `make jm-apply`), and pinned in C by generating all 1023 codes and requiring them distinct -- a caller sizing a code-assignment scheme allocates against that number.

- **F2 · FIXED** — **Three contract claims had nothing behind them.** `max_out` was never exercised: every existing call passed `max_out == n`, so *"emission stops there"* and *"@return min(n, max_out)"* were prose. Nor was *"requesting more than one period is valid -- the sequence simply wraps"*, nor `gold_destroy(NULL)` as a documented no-op. All three are now pinned in `test_gold_core.c`, with the capacity case also requiring the untouched tail of the caller's buffer to stay untouched and a zero capacity to advance neither LFSR.

- **F3 · FIXED** — **"Preferred pair" is a claim about the family, and one pair was checked.** The C test cross-correlated exactly two members, which says nothing about the other 1021 -- the property that makes the set usable for multiple access is that it holds between ANY two. Now sampled at six members (15 pairs) in C and at 12 members (66 pairs) here, every value landing in the three-valued set. Worth noting what this still is: a sample. The exhaustive claim is 1023x1022/2 pairs and neither gate pays for it.

- **F4 · GAP** — **The constants are certified against the published vector, not against the document.** CCSDS 415.0-G-1 is not in the repository's standards library (`~/refs/` holds 130.1-G-3 and 131.0-B-6), so nothing here can confirm that taps 934/567 and seed 73 are what section 5.2.2.4 specifies -- only that they reproduce the Figure 5-2 worked example, Code #365, chip for chip. That is strong evidence and it is not the same thing: a transcription error that happened to preserve that one code would pass. Tracked as [gh-1133](https://github.com/doppler-dsp/doppler/issues/1133) -- the fix is to add the document to the standards library and check the polynomials against it directly.

## 4. Limits -- the certified envelope

Claims a caller may rely on. A failure here is a regression, not a new finding. Every one is asserted by `src/doppler/wfm/tests/test_validation_limits.py`.


| verdict | claim |
|---|---|
| PASS | exactly 1023 seeds are usable -- 2^length - 1, because a zero seed is refused rather than silently accepted |
| PASS | and they reach 1023 DISTINCT codes, one per seed: the number a code-assignment scheme may allocate against |
| PASS | which is 2^n - 1 of the family's 2^n + 1 members; the two constituent m-sequences are not reachable from any seed |
| PASS | no sampled member is a cyclic shift of another (12 members compared), so the count is codes and not phases |
| PASS | every correlation value across 66 pairs and a full autocorrelation falls in a set of exactly three |
| PASS | and that set is exactly {-1, -65, 63}, as the header states |
| PASS | which is the theoretical {-1, -t, t-2} for t(n) = 2^((n+2)/2) + 1 = 65 at n=10 -- derived, so the check still means something at another length |
| PASS | the autocorrelation peak is exactly 1023 at zero lag |
| PASS | and the worst sidelobe anywhere is 65 -- the near-far bound a receiver must be designed to |
| PASS | giving a peak-to-worst-sidelobe margin of 23.9 dB |
| PASS | against a plain m-sequence's worst sidelobe of 1: the purity Gold trades away is real and measured, not asserted |
| PASS | a 36.3 dB difference -- so a single-transmitter link should prefer the m-sequence, and a shared band should not |
| PASS | the default configuration reproduces CCSDS 415.0-G-1 Figure 5-2's worked example (PN Code Library #365) chip for chip |
| PASS | and the code is balanced: 512 ones to 511 zeros |
| PASS | generating past one period wraps exactly, so a caller may ask for any length without tracking the boundary |
| PASS | reset() restarts from chip 0, reproducing a full period |
| PASS | and a serialized blob resumes a mid-code generator bit-exactly, carrying BOTH registers |


## 5. Summary

- **4 findings**, 1 of them gaps or confirmed defects: F4
- **17/17 limits** hold
