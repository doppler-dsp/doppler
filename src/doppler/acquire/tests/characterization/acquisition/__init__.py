"""Characterization of `Acquisition` across its envelope.

`characterize.py` sweeps the same axes the composed-receiver subject
uses, so the two reports are comparable: an acquisition boundary means
something only against the receiver's own pull-in. See the parent
package for why this is not an example.
"""
