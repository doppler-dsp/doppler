# Burn-down: stateful objects not yet serializable. An entry is removed when it
# gains `serializable = "true"` + the dp_state.h triplet. This file is now EMPTY:
# the rollout is complete — every stateful object is serializable, and the gate
# (scripts/check_serializable.py) requires every new object to declare
# serializable = "true" or be listed in scripts/.serializable-stateless.
