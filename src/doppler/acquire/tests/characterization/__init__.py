"""Characterization runs for `doppler.acquire`'s objects.

Long envelope sweeps, run by `make characterize` rather than per push;
each subject keeps a fast pytest twin in `src/doppler/acquire/tests/`.
What this category is, what runs it and why is written once, in
`src/doppler/dsss/tests/characterization/__init__.py`.
"""
