# BurstCapture — validation report

Generated by `validate.py` in this folder. Every number is measured from the C implementation through its own binding; nothing is modelled. Re-run to regenerate.

## Executive summary

- **Status.** **CERTIFIED** — all 22 limits hold.
- **Findings.** 6 recorded, 1 still open: F6.

**What a caller most needs to know**

- **`preamble_start` is exact, and that is the object's reason to exist.** Acquisition's `code_phase` is a lag modulo one code period and an epoch error is a cliff rather than a gradient; the reported start is correct to the sample at every offset tested, including ones off the code-period grid (§2.2).
- **Leave more dead air than the header's formula says.** It gives `max(0, refine_span - burst_len)`; measured, a pair needs 582 samples before both bursts are reliably captured (§2.4, F1).
- **Filter on `cn0_dbhz_est`, not `refine_margin`.** A spurious window is expected at `pfa = 1e-3` and this object will not gate on quality; C/N0 separates the two populations by 14.3 dB while the margin separates them by hundredths (§2.5, F2).
- **Block size is not a parameter of the answer.** From 333 samples to a push larger than the ring, the windows are bit-identical and nothing is dropped (§2.3).
- **The persistent flavour costs nothing and saves the checkpoint.** Its blob is smaller by exactly the retained span (41,408 B here), the ring's pages ARE the file's, and the measured throughput is within noise of the anonymous ring (§2.6).

## 1. The object

`BurstCapture` sits between a detector and whatever consumes a burst. Acquisition reports an END anchor and a `code_phase` that is a lag **modulo one code period**, so it fixes the alignment within a preamble repetition and never says WHICH one; a burst has a frame that begins in one specific repetition. This object resolves that, keeps the look-back needed to reach a start already gone past, and emits the burst's samples. It stops there.

- Design: [`burst-capture.md`](../../../../../../docs/design/burst-capture.md)
- Header: `native/inc/burst_capture/burst_capture_core.h`
- C tests: `native/tests/test_burst_capture_core.c`
- Characterization: `src/doppler/dsss/tests/characterization/burst_capture/`

## 2. Characterisation

### 2.1 The geometry is derived, never asked for (C §1)

A caller asked to size a look-back buffer is a caller handed a way to lose bursts silently, so every span comes from the geometry. The two a caller must nevertheless RESPECT are read back rather than recomputed — restating `(k_lo+k_hi+reps)·code_period` in a caller is the drift this repo forbids, and the header's own prose for it was 2.4x low until it was measured.

| quantity | value (samples) | identity |
|---|---|---|
| `burst_len` | 2448 | what gets captured |
| `refine_span` | 2728 | the coalescing reach |
| `retain_span` | 5176 | `refine_span` + one burst |


`retain_span == refine_span + burst_len` holds exactly (5176 = 2728 + 2448). Nothing here is a constant that happens to fit the test geometry: a capture built for a burst 20x longer derives `retain_span` 51,688 against 5,176, and the detection queue is sized from `burst_len / refine_span` for the same reason — a fixed depth silently dropped the hit AND the rest of its batch on any geometry but the one the tests happened to use.

### 2.2 The epoch: never late, and exact (C §3)

The claim the object exists for. `code_phase` measures `burst_start mod code_bins` exactly, so a phase seed resolves alignment WITHIN a code period and never which period — and an epoch error is a cliff, not a gradient: a window one repetition out decodes at half the payload in error. Scored against the burst's known position, not against the object's opinion of itself.

| burst placed at | reported `preamble_start` | error (samples) |
|---|---|---|
| 9000 | 9000 | 0 |
| 23117 | 23117 | 0 |
| 41000 | 41000 | 0 |
| 60500 | 60500 | 0 |


Zero at every offset, including ones that are not multiples of the code period — which is what distinguishes a resolved epoch from a phase seed that happened to land.

### 2.3 Block size is not a parameter of the answer (C §5, §11)

The ring is a contiguous window over the stream and is never reset between bursts, so a burst whose tail falls outside one call is completed by a later one. A push LARGER than the ring is sliced rather than refused, which is what accepting any block size costs.

| block size | windows | identical to 333-sample run | dropped |
|---|---|---|---|
| 333 | 5 | yes | 0 |
| 4096 | 5 | yes | 0 |
| 65536 | 5 | yes | 0 |
| 200000 | 5 | yes | 0 |


### 2.4 The spacing floor — measured, against a derived claim

The object DERIVES this now, as `min_gap`, so a caller never applies the rule by hand. The derivation: a detection's anchor is the code epoch of whichever frame detected, and acquisition's framing is not aligned to the preamble, so the last frame that can detect sits `reps * code_period` past the true start. CLAIM merges two anchors closer than `refine_span`, so the first burst detected LATE and the second EARLY close by that much before CLAIM sees them:

        min_gap = max(0, refine_span + reps*code_period - burst_len)

**Two numbers here were wrong before this pass and are worth recording.** The prose said `max(0, refine_span - burst_len)`, short by the whole detection-lag term — 32 samples against 528. And this report's own first answer, 256, came from a **12-trial** sweep that read 100%; at 60 trials the same spacing is 88.8%. A floor measured with too few alignments is not a floor (doppler#1172).

| dead air (samples) | transmitted bursts captured |
|---|---|
| 0 | 50% |
| 194 | 67% |
| 388 | 83% |
| 582 | 100% |
| 776 | 100% |
| 1552 | 100% |
| 3104 | 100% |
| 6208 | 100% |


The header's number for this geometry is **280 samples**. Measured, the pair is not reliably captured until **582 samples** of dead air — 2x that, and about 4.7 code periods. Recorded as F1.

### 2.5 Telling a real window from a spurious one

At `pfa = 1e-3` over a surface this size a false alarm is expected, not a defect — this object is a detector's output stage and gating it on signal quality would make it lie about what it found. So the caller filters, and the question a report has to answer is **with which read-back**.

| statistic | at a real burst | at a spurious window | separation |
|---|---|---|---|
| `cn0_dbhz_est` (dB-Hz) | 64.7 | 50.4 | 14.3 dB |
| `refine_margin` | 0.807 | 0.931 | 0.124 |


Over 84 real and 2 spurious windows. The answer is `cn0_dbhz_est`, and it is not the intuitive one — `refine_margin` is the stage's own health signal and separates the two populations by a few hundredths, because a window on noise has no period to resolve and scores much like one that resolved it. Recorded as F2.

### 2.6 The two flavours' blobs (C §12–§15)

The look-back IS the checkpoint for an in-RAM capture. Backed by a file, the ring's pages are the file's contents (`MAP_SHARED`), so the blob names where in the ring the history sits instead of carrying it.

| flavour | `state_bytes()` | carries the look-back? |
|---|---|---|
| `BurstCapture` | 46,104 B | yes |
| `PersistentBurstCapture` | 4,696 B | no — the file does |


The difference is 41,408 B, which is exactly `retain_span * 8` (5176 complex64 samples) — the whole of the retained history and nothing else.

### 2.7 The lifecycle, and what the read-backs mean (C §2, §6–§10)

The claims a caller relies on between pushes rather than inside one. Each is small; together they are the difference between an object a caller can drive and one it has to guess at.

| claim | holds |
|---|---|
| `push()` returns whole windows only | True |
| `events()` has one row per window | True |
| a burst one sample short is HELD, `pending == 1` | True |
| `reset()` returns to searching and keeps working | True |
| `n_bursts` is a lifetime count and survives reset | True |
| `state_bytes()` does not move with the stream | True |
| the read-backs agree with the event row | True |
| a grid deeper than `reps` is refused | True |
| the search under the capture is visible | True |
| an unmeetable `pd` warns AND reads back | True |
| `detections()` has at least one row per window | True |
| every window has a detection epoch within `refine_span` | True |


## 3. Review — findings

- **F1 · FIXED** — **The documented required gap was short by the whole detection-lag term, and the first measurement of it was under-powered.** The prose said `max(0, refine_span - burst_len)` = 280 samples here; the object now DERIVES `min_gap` = 776 from the claim rule and the detection lag, so a caller applies no rule at all (§2.4). Two things were wrong: the formula omitted `reps * code_period`, and this report's own first answer (256) came from a 12-trial sweep reading 100% where 60 trials read 88.8%. The derivation is checked on four geometries, each predicting a different value. [gh-1172](https://github.com/doppler-dsp/doppler/issues/1172).

- **F2 · BY DESIGN** — **`refine_margin` is not the statistic to filter on; `cn0_dbhz_est` is.** The margin separates real from spurious windows by 0.124 against C/N0's 14.3 dB (§2.5). That reads as a defect and is not one: the margin answers 'was the code PERIOD resolved', and a window sitting on noise has no period to resolve, so it scores much like one that did. It is a hand-off health signal, which is a different question from 'is anything there'.

- **F3 · FIXED** — **`reset()` left the ring behind the stream.** `head`/`tail` are monotonic ABSOLUTE counters, so emptying the ring by consuming everything available left them at the stream's last position while `samples_fed` restarted at 0 — every position computed afterwards was 0-based against a ring that was not, so nothing was reachable, refine never ran and writes were refused. Found by asserting a re-push after reset, which the code this was moved from never did. Fixed here; the receiver's own copy is [gh-1169](https://github.com/doppler-dsp/doppler/issues/1169).

- **F4 · FIXED** — **`configure_search_raw` returned a code its header did not document.** The composed child forwards `acq`'s own `-1`, which is not one of the eight codes `clib_common.h` defines, while this object's header promised `DP_ERR_INVALID` (-4). A C caller branching on the documented code would have mis-read a refusal. Translated at this boundary rather than weakening the doc to match.

- **F5 · C-ONLY** — **The zero-copy consumer face is not reachable from Python.** `burst_capture_ready`/`window`/`event_at` let a composing C object borrow a window out of the scratch instead of copying it again, which is what keeps `DsssBurstReceiver` paying one memcpy per burst rather than two. The binding necessarily copies into a numpy array, so the borrow is certified in C §7 and §3 instead.

- **F6 · GAP** — **The per-burst window copy is measured only at a short-burst geometry.** `bench_burst_capture_core` puts it at 34 µs for four bursts against a 2.24 ms search floor — 1.5% — but the copy scales with `burst_len` and the search floor does not, so a real link geometry (a 2.5 MB window rather than 20 kB) is a different measurement that has not been taken. Tracked as [gh-1173](https://github.com/doppler-dsp/doppler/issues/1173).

## 4. Limits — the certified envelope

Claims a caller may rely on. A failure here is a regression, not a new finding.


| verdict | claim |
|---|---|
| PASS | `retain_span` == `refine_span` + `burst_len` (5176 = 2728 + 2448) |
| PASS | `preamble_start` is EXACT at every tested offset, including ones that are not a multiple of the code period (§2.2) |
| PASS | the windows are bit-identical across block sizes from 333 samples to a push 1.6x the ring's capacity (§2.3) |
| PASS | no sample is dropped by the history ring at any block size (§2.3) |
| PASS | `min_gap` is DERIVED — `refine_span + reps*code_period - burst_len` = 776 — rather than a constant that fits this geometry (§2.4) |
| PASS | the sweep reaches 100% at 582 samples, at or below the derived `min_gap` of 776 — so the bound the object publishes is sufficient, not merely plausible (§2.4) |
| PASS | `cn0_dbhz_est` separates a real window from a spurious one by more than 3 dB, so it is usable as the filter (§2.5) |
| PASS | a blob taken mid-preamble resumes into a FRESH instance and finds the burst it was holding — the retained look-back travels (§2.6) |
| PASS | the persistent flavour's blob is smaller by exactly the retained span (41,408 B) (§2.6) |
| PASS | a blob does not travel between the in-RAM and persistent flavours in either direction (§2.6) |
| PASS | `push()` returns a whole number of windows, never a partial one — half a burst is not a burst (§2.7) |
| PASS | `events()` returns exactly one row per window `push()` returned, so a caller can index them together (§2.7) |
| PASS | a burst one sample short of complete is HELD with `pending == 1`, and one more sample releases it (§2.7) |
| PASS | `reset()` returns to the searching state AND the same stream is found again afterwards — the assertion the code this was moved from never made (§2.7, F3) |
| PASS | `n_bursts` is a LIFETIME count and survives `reset()`, so a reset cannot report a clean stream (§2.7) |
| PASS | `state_bytes()` is a pure function of configuration — it does not move with the stream, so a blob restores into any instance of the same geometry (§2.7) |
| PASS | the scalar read-backs and the event row describe the same burst, which is what lets a caller holding one window skip the list (§2.7) |
| PASS | `configure_search_raw` accepts a coherent depth within `reps` and REFUSES one beyond it, rather than silently clamping (§2.7) |
| PASS | the search is visible as numbers — `doppler_bins`, `n_noncoh`, `code_bins`, `doppler_span_hz`, both gates and `straddle_loss` — so a caller can size a link without inferring it; with no design C/N0 it is the whole preamble in ONE look, and never underpowered (§2.7) |
| PASS | a search that cannot meet the requested `pd` warns at construction AND reads back as `underpowered`, rather than building quietly and capturing fewer bursts than arrived (§2.7) |
| PASS | `doppler_res_hz` reports acquisition's native bin width, so the event's Doppler estimate carries its own uncertainty (§2.7) |
| PASS | `detections()` is what the SEARCH found — at least one row per window, each epoch stream-absolute (within `refine_span` of the burst it became) — so a bank reads both faces from one engine (§2.7) |


## 5. Summary

- **6 findings**, 1 of them gaps or confirmed defects: F6
- **22/22 limits** hold
