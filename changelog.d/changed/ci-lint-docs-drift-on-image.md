- **The lint, docs and manifest-drift CI jobs run on the pinned CI image**
    ([#1524](https://github.com/doppler-dsp/doppler/issues/1524)), like
    every build and test job: one toolchain, pinned by digest.
