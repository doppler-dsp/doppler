# Relocatable: derive the prefix from this file's own location
# (<prefix>/<libdir>/pkgconfig/doppler.pc), so an extracted release tarball
# works wherever it lands — not only the build-time CMAKE_INSTALL_PREFIX.
prefix=${pcfiledir}/../..
exec_prefix=${prefix}
libdir=${exec_prefix}/@CMAKE_INSTALL_LIBDIR@
includedir=${prefix}/@CMAKE_INSTALL_INCLUDEDIR@

Name: doppler
Description: doppler C library
Version: @PROJECT_VERSION@
Libs: -L${libdir} -ldoppler
# libdoppler is pure C and links only -lm (no C++ runtime, no zmq). The optional
# ZMQ/stream layer lives in libdoppler_stream (link -ldoppler_stream -lstdc++
# -lpthread for it); it is intentionally not part of the core .pc.
Libs.private: -lm
Cflags: -I${includedir}
