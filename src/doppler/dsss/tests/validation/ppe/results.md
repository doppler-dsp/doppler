# PolynomialPhaseEstimator — validation report

Generated by `validate.py` in this folder. Every number is measured from the C implementation through its own binding; nothing is modelled. Re-run to regenerate.

## Executive summary

- **Status.** **CERTIFIED** — all 15 limits hold.
- **Findings.** 5 recorded, none open.

**What a caller most needs to know**

- **It resolves a tone to 0e+00 of an FFT bin**, and stays inside 0.05 of a bin at 0 dB input SNR. That precision is the object's whole value — a burst gets one estimate with no loop behind it (§2.1, §2.5).
- **`snr_db` is not an input SNR.** It is a peak-to-mean after coherent integration, so it carries the processing gain — quadrupling the segment adds 6.0 dB on identical input. A confidence threshold set from it moves whenever the segment length does (§2.4, F3).
- **`nfft` is 4x next_pow2(max_len), not next_pow2.** The header said otherwise, and three buffers scale with it — so memory budgeted from the old comment was out by four (§2.3, F2).
- **`max_rate = 0` returns rate exactly 0.0**, not a small number, so the pure-Doppler case is testable by equality (§2.2).
- **Nothing here is stateful.** No triplet, no resume, and `reset()` is asserted to be the no-op it claims to be — which is what stops it quietly acquiring behaviour (§2.7, F5).

# PolynomialPhaseEstimator — validation report

## 1. The object — one shot, no loop behind it

`PolynomialPhaseEstimator` estimates a normalized frequency and chirp rate from one segment, by a coherent search over (chirp rate x frequency). It is stateless and by-value: every estimate depends only on the samples handed to that call.

That statelessness is why this report has no resume section and no lifecycle sequence — there is nothing to serialize, and `reset()` is documented as a no-op. What is left is accuracy, and accuracy is the whole of the object's value: `BurstDemod` gets one frequency estimate per burst with no tracking loop to walk the residual out afterwards.

### 1.1 The claim inventory

Step 1 of `docs/dev/contributing/validation.md`. The C test is `test_ppe_core.c`, which was 74 lines. Four claims were untestable at its tolerances or absent entirely, and one field-level claim was **wrong**.

| header claim | pinned where | here |
|---|---|---|
| the peak is refined sub-bin by parabolic interpolation | **tolerance was 2.6 BINS** — would pass on the raw argmax | §2.1 |
| the rate axis is searched coherently and refined sub-grid | C, at four rates | §2.2 |
| `max_rate = 0` collapses to a single FFT, rate forced to 0 | C | §2.2 |
| `nfft` is the zero-padded transform length | **the header said next-pow2; it is 4x that** | §2.3, F2 |
| `snr_db` is the winning-row peak-to-mean | **was nothing** — now C, as a scaling relationship | §2.4 |
| matched-filter optimal, holds at low SNR | **was nothing** — now measured across input SNR | §2.5 |
| `freq_norm` lies in `[-0.5, 0.5)` | **was nothing** — now C, at both shoulders | §2.6 |
| `n_in` in `[4, max_len]`; out of range zeroes every field | **upper bound only** — the floor is now checked too | §2.6 |
| `reset()` is a no-op | **was nothing** — now C | §2.7 |
| `max_len >= 4`, `max_rate >= 0`, else NULL | C | §2.7 |

## 2. Characterisation

Measured behaviour. No verdicts — those are §3.

### 2.1 Sub-bin frequency accuracy

At `max_len = 512` the transform is 4x zero-padded, so a *grid* step is 1/(4L). The claim worth measuring is against the **FFT bin** of the segment itself, 1/L = 1.953e-03, because that is what a caller would get with no refinement at all. Errors below are in units of that bin.

| offset into the bin | true | estimate | error (bins) |
|---|---|---|---|
| 0.000 | 0.07812500 | 0.07812500 | 0.00e+00 |
| 0.125 | 0.07836914 | 0.07836914 | 0.00e+00 |
| 0.250 | 0.07861328 | 0.07861328 | 0.00e+00 |
| 0.375 | 0.07885742 | 0.07885742 | 0.00e+00 |
| 0.500 | 0.07910156 | 0.07910156 | 0.00e+00 |
| 0.625 | 0.07934570 | 0.07934570 | 0.00e+00 |
| 0.750 | 0.07958984 | 0.07958984 | 0.00e+00 |
| 0.875 | 0.07983398 | 0.07983398 | 0.00e+00 |

Worst **0.00e+00 bins** across the bin, noiseless. The C test tolerated 2.6 bins, which is four orders of magnitude looser than the object achieves and wide enough to pass with the refinement deleted — that is F1. Raw sweep: `data/subbin.csv`.

### 2.2 The chirp-rate axis, and its collapse at max_rate = 0

| true rate | estimate | error (grid steps) |
|---|---|---|
| +0.00e+00 | -5.5446e-10 | 0.000 |
| +5.00e-06 | +5.0006e-06 | 0.000 |
| +1.00e-05 | +1.0000e-05 | 0.000 |
| +2.00e-05 | +2.0000e-05 | 0.000 |
| -1.50e-05 | -1.5000e-05 | 0.000 |

`n_rate = 53` hypotheses over +/-5e-05, a grid step of 1.92e-06. Worst error **0.000 of a step** — the parabolic refinement on the rate axis is what makes a coarse grid sufficient, and a zero rate lands on a node because the count is forced odd.

With `max_rate = 0` the rate axis collapses to `n_rate = 1` and the returned rate is **exactly** 0.0, not merely small — a caller can test it for equality.

### 2.3 The transform is 4x zero-padded, not next-pow2

| max_len | next pow2 | nfft | ratio |
|---|---|---|---|
| 100 | 128 | 512 | 4x |
| 512 | 512 | 2048 | 4x |
| 800 | 1024 | 4096 | 4x |

The header documented this field as *"next pow2 of max_len"*. It is four times that, and the difference is not cosmetic: `nfft` sizes three buffers, so a caller budgeting memory from the header was out by 4x — and the same 4x is what makes §2.1 as accurate as it is. Corrected and pinned (F2).

### 2.4 `snr_db` is a post-integration quantity

The field is documented only as a *"winning-row peak-to-mean (rough confidence)"*, and nothing measured it. It is a peak-to-mean taken **after** the coherent transform, so it carries the processing gain: on the same input it grows with the segment length. A caller thresholding on it without knowing that is comparing an integrated number against an input-referred one.

| segment length | 10log10(L) | snr_db at 0 dB input |
|---|---|---|
| 256 | 24.08 | 23.90 |
| 512 | 27.09 | 27.08 |
| 1024 | 30.10 | 29.93 |

Quadrupling the segment adds **6.03 dB**, against the 6.02 dB of ideal coherent gain — the shortfall is the window's noise-equivalent bandwidth and the fact that the surface mean is not exactly the noise floor. Asserted as a relationship rather than a literal: a literal would pin the noise draw as much as the estimator.

### 2.5 Where the estimate degrades

The header calls the estimator matched-filter optimal and says it holds at low SNR, which nothing measured. This is the envelope a caller sizing a burst preamble actually needs.

| input SNR (dB) | median error (bins) | worst of 8 (bins) |
|---|---|---|
| +30 | 0.0005 | 0.0008 |
| +20 | 0.0004 | 0.0062 |
| +10 | 0.0041 | 0.0183 |
| +0 | 0.0192 | 0.0502 |
| -10 | 0.0486 | 0.1382 |

Still inside **0.050 of a bin** at 0 dB input SNR over eight noise draws, degrading gracefully rather than breaking — the estimate stays on the right peak and loses precision. Raw sweep: `data/low_snr.csv`.

### 2.6 The documented ranges

| true | estimate | in [-0.5, 0.5) | sign preserved |
|---|---|---|---|
| +0.450 | +0.450000 | True | True |
| +0.499 | +0.499000 | True | True |
| -0.450 | -0.450000 | True | True |
| -0.499 | -0.499000 | True | True |

Accurate to 1.9e-07 at both shoulders with the sign intact. The sign check is not redundant: an off-by-one in the bin-to-frequency mapping wraps a near-Nyquist tone to the opposite side, which is the failure that reads downstream as a receiver locking to the negative image.

Below the documented floor of 4 samples every field is zeroed (**True**), and the floor itself estimates (**True**) — so the refusal is a boundary and not a blanket.

### 2.7 reset, and what it refuses to build

| claim | result |
|---|---|
| an estimate is bit-identical either side of reset() | True |
| max_len < 4 and max_rate < 0 are refused | True |

The reset check is not ceremony: it rules out a `reset()` that clears scratch the next estimate depends on, which is the way a documented no-op stops being one.

### 2.8 Invariances, and the M-th-power contract

The search runs on a magnitude surface, so a constant phase or a constant amplitude must not move the estimate. These are cheap to state and worth pinning because a normalisation added later for numerical reasons is exactly what would break them quietly.

| transform | d freq (bins) | d rate | d snr_db |
|---|---|---|---|
| phase rotated 1.1 rad | 0.0e+00 | 0.0e+00 | 0.000 |
| amplitude x 100 | 0.0e+00 | 0.0e+00 | 0.001 |
| amplitude x 0.01 | 0.0e+00 | 1.4e-10 | 0.001 |

`snr_db` is invariant too, which follows from it being a peak-to-MEAN in dB rather than an absolute level — a scale factor cancels.

The header also states a contract the caller must honour: a non-data-aided caller raises an M-PSK stream to the M-th power to strip the modulation, **which scales both returned values by M**, so the caller halves them for BPSK. That is a claim about this object's output and nothing measured it.

| quantity | true | squared estimate | halved | error (bins) |
|---|---|---|---|---|
| frequency | 0.030000 | 0.060079 | 0.030039 | 0.020 |
| chirp rate | 4.000e-06 | 8.000e-06 | 4.000e-06 | — |

Squaring a BPSK stream recovers 2f and 2r; halved, the frequency is within **0.020 of a bin** of truth. The residual is the squaring itself — it doubles the noise and the spurs along with the signal — not the estimator.

| max_rate | n_rate | odd |
|---|---|---|
| 1e-05 | 11 | True |
| 5e-05 | 53 | True |
| 1e-04 | 105 | True |
| 5e-04 | 525 | True |

The count is forced odd on purpose: it puts `r = 0` on a grid node, so a genuinely unchirped signal is found at a hypothesis rather than between two — which is the common case and the one a near-static Doppler caller lands on.

## 3. Review — findings

- **F1 · FIXED** — **The sub-bin refinement was pinned at a tolerance 2.6 BINS wide.** The header claims the peak is refined sub-bin by parabolic interpolation; the C test's `ftol` of 5e-3 against a bin of 1/512 = 1.95e-3 is 2.6 bins, so the section would have passed with the refinement deleted and the raw argmax returned. Measured, the estimator resolves a noiseless tone to 0e+00 of a bin, and the gate is now 0.01 bins — a hundred times tighter than the old tolerance and a hundred times looser than the object achieves. Sabotage-proven by forcing the raw-argmax fallback branch, which takes it red and leaves every pre-existing assertion green.

- **F2 · FIXED** — **The `nfft` field comment was wrong by 4x.** It documented the transform length as *"next pow2 of max_len"*; the implementation uses `next_pow2 (max_len) << 2`. Not cosmetic: `nfft` sizes `buf`, `spec` and `mag`, so a caller budgeting memory from the header was out by a factor of four on three buffers. The 4x is also what makes the sub-bin accuracy in §2.1 what it is, so the comment was hiding the mechanism as well as the footprint. Corrected, and pinned in C so the two cannot drift apart again.

- **F3 · FIXED** — **`snr_db` was measured by nothing**, in either language, and it is not an input-referred SNR. It is a peak-to-mean taken after the coherent transform, so it carries the processing gain: quadrupling the segment adds 6.0 dB on identical input (§2.4). A caller thresholding on it as though it were the segment's SNR is comparing an integrated quantity against an input-referred one, and the threshold then moves whenever the segment length does. Now asserted as that scaling relationship rather than as a literal, which would pin the noise draw as much as the estimator.

- **F4 · BY DESIGN** — The frequency axis's sub-bin refinement is **not in this object**: `ppe_estimate` delegates to `find_peaks_f32` from `spectral_core` and only falls back to a raw argmax if that returns nothing. The header's "refined sub-bin in both axes by parabolic interpolation" is therefore accurate about the behaviour and quiet about the ownership — the rate axis is refined here, the frequency axis is refined by a shared primitive. Recorded rather than changed, because the composition is correct and re-inlining the interpolation would be exactly the duplication the library's own rule forbids. The consequence worth knowing: a regression in `find_peaks_f32` shows up as a ppe accuracy failure.

- **F5 · BY DESIGN** — There is no state triplet and no `serializable` flag, and that is correct rather than an omission: the object is a by-value analyzer that computes each estimate purely from the segment it is handed, so there is nothing to checkpoint. `reset()` exists only to satisfy the common object protocol and is now asserted to be the no-op it claims to be (§2.7) — which is what stops it quietly acquiring behaviour later.

## 4. Limits — the certified envelope

Claims a caller may rely on. A failure here is a regression, not a new finding. Every one is asserted by `src/doppler/dsss/tests/test_validation_limits.py`.


| verdict | claim |
|---|---|
| PASS | a noiseless tone is resolved to 0.0e+00 of an FFT bin, across the whole bin |
| PASS | the chirp rate is resolved to 0.000 of a rate-grid step at five rates including zero and both signs |
| PASS | max_rate = 0 collapses to one FFT and returns rate EXACTLY 0.0, so a caller may compare it for equality |
| PASS | nfft is 4 * next_pow2(max_len) at three lengths — the documented footprint, corrected |
| PASS | snr_db carries the coherent processing gain: quadrupling the segment adds 6.0 dB on identical input |
| PASS | the estimate stays within 0.050 of a bin down to 0 dB input SNR over eight noise draws |
| PASS | freq_norm stays in [-0.5, 0.5) with the sign intact at both shoulders — a near-Nyquist tone does not wrap to its image |
| PASS | ...and is accurate to 1.9e-07 there |
| PASS | a segment shorter than the documented floor of 4 zeroes every field rather than estimating |
| PASS | ...and the floor itself estimates, so the refusal is a boundary and not a blanket |
| PASS | an estimate is bit-identical either side of reset(), so the documented no-op is one |
| PASS | max_len < 4 and max_rate < 0 are refused at construction |
| PASS | the estimate is invariant to a constant phase and to amplitude scaling over four decades, snr_db included |
| PASS | squaring a BPSK stream returns 2f and 2r: halved, the frequency is within 0.020 of a bin of truth |
| PASS | n_rate is odd at every max_rate, so r = 0 is a grid node rather than a point between two hypotheses |


## 5. Summary

- **5 findings**, 0 of them gaps or confirmed defects — none left
- **15/15 limits** hold
- Raw sweeps: `data/subbin.csv`, `data/rate.csv`, `data/snr_gain.csv`, `data/low_snr.csv`
