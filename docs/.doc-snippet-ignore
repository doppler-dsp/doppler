# doc-snippet drift gate — burn-down backlog (see docs/dev/doc-examples.md)
#
# Pages listed here are NOT yet gated by src/doppler/tests/test_doc_snippets.py.
# This is a TEMPORARY backlog that must shrink to empty: as each page's Python
# fences are made to run (or marked skip=), delete its line. A NEW page is never
# added here — it is gated on arrival (fail-closed). Mirrors the burn-down
# idiom of docs/api/.api-coverage-ignore and scripts/.serializable-ignore.
